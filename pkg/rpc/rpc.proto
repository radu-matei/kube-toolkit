syntax = "proto3";

package rpc;

service Joker {
    rpc GetVersion(Empty) returns (Version){}
}

message Version {
	string sem_ver = 1;
	string git_commit = 2;
}


// TODO - remove this message type and use google.protobuf.Empty
message Empty {}
